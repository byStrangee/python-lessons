class FirstClass:
	__init__(self, size, age):
		self.size = size
		self.age = age
	get_name():
		print(this.size, this.age)

a = FirstClass("Hello", 15)

class Another:
	__init__(self, action, user, info):
		self.another = {}
		self.another['action'] = action
		self.another['user'] = user
		self.another['info'] = info
	printAll():
		print(self)
		
print(
	Another('some', {"name": "age", "age": "name"}, 'Lorem')
)
		
